<!DOCTYPE html>
<html lang="en">

<%- include("./partials/head") %>
<link rel="stylesheet" href="/css/miPerfil.css">

<body>

    <% if (user != undefined ) { %>
    <%- include("./partials/headerLogueado") %>
    <% } else { %>
    <%- include("./partials/headerDeslogueado") %>
    <% } %>

    <main>
        <div class="uk-section uk-section-small">
            <div class="uk-container uk-container-small">
                <section class="info-usuario uk-flex uk-flex-around uk-width-1-1">
                    <div class="uk-width-1-3 uk-flex uk-flex-center uk-flex-middle">
                        <% if (user.fotoPerfil != "") { %>
                        <img src="/images/avatars/<%= user.fotoPerfil %>" alt="<%= user.fotoPerfil %> "
                            class="fotoo-perfil">
                        <% } else {%>
                        <img id="fotoPerfil" src="/images/avatars/avatar.png" alt="avatar">
                        <% } %>
                    </div>
                    <div class="uk-width-2-3 uk-flex uk-flex-column uk-flex-around">
                        <div class="uk-flex uk-flex-middle">
                            <h3 class="uk-margin-remove"><%= user.nombreDeUsuario%> </h3>
                            <a href="/user/editarPerfil" uk-icon="icon:cog; ratio: 1.2" class="uk-margin-left"></a>
                        </div>
                        <div>
                            <ul class="red-usuario uk-flex uk-flex-between uk-width-1-2">
                                <li class="numpubli"><%=user.posteos.length%> posts</li>
                                <li class="numpubli"><%= user.seguidor.length %> seguidores</li>
                                <li class="numpubli"><%= user.seguido.length %> seguidos</li>
                            </ul>
                        </div>
                    </div>

                </section>

                <section class="muro uk-flex uk-flex-middle uk-flex-center">
                    <div class="uk-grid uk-child-width-1-3 uk-width-1-1 uk-margin-remove" uk-grid>
                        <% let postDeUsuario = []; %>
                        <%for(let i = 0; i < user.posteos.length; i++) { %>
                        <div
                            class="uk-card uk-card-hover uk-card-body uk-flex uk-flex-column uk-flex-middle uk-flex-center ">
                            <a href="/detallePost/<%=user.posteos[i].id%> " class="link-detalle">
                                <img src="/images/posts/<%= user.posteos[i].imagen%>"
                                    alt="<%= user.posteos[i].imagen %> " class="foto-perfil">
                                <p class="uk-text-center colornegro"><%= user.posteos[i].descripcion%> </p>
                            </a>
                        </div>
                        <% } %>
                    </div>
                </section>
            </div>
        </div>
    </main>

    <%- include("./partials/footer") %>

</body>

</html>